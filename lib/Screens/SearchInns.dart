import 'package:flutter/material.dart';

class searchInns extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text("Hello"),
    );
  }
}
